---
features:
  - |
    :external:class:`~qiskit_ibm_runtime.results.PauliLindbladError`
    objects can now be embedded in slices of ``QuantumCircuit`` using the
    :class:`~qiskit_addon_obp.utils.noise.PauliLindbladErrorInstruction`
    and will be handled properly by :func:`~qiskit_addon_obp.backpropagate`.
